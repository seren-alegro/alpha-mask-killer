READ FUCKING README








Linux / mac

```
cd alpha mask killervis
```

make fucking virtual

```
python -m venv venv
source venv/bin/activate
```

we need Pillow
```
pip install Pillow
```

and..
```
python main.py
```

windows

go to alpha mask killervis folder
and

make fucking virtual

```
python -m venv venv
venv\Scripts\activate.bat
```

we need Pillow
```
pip install Pillow
```

and..
```
python main.py
```

fucking poweshell?

go to alpha mask killervis folder
and

make fucking virtual

```
python -m venv venv
.\venv\Scripts\Activate.ps1
```

we need Pillow
```
pip install Pillow
```

and..
```
python main.py
```
